name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build, Analyze, and Deploy
    runs-on: ubuntu-latest

    steps:

      # Checkout code
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      # Set up JDK 21
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      # Build and SonarQube analysis
      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: mvn -B verify sonar:sonar -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} -Dsonar.token=${{ secrets.SONAR_TOKEN }}

      # Build Docker image
      - name: Build Docker image
        run: |
          docker build -t carikk/quality-gate:latest .

      # OPTIONNEL : Push Docker image only if you have Docker Hub credentials
      # Uncomment these lines only if you have set DOCKER_USERNAME and DOCKER_PASSWORD secrets
      # - name: Log in to DockerHub
      #   shell: powershell
      #   run: |
      #     docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"
      
      # - name: Push Docker image
      #   run: |
      #     docker push carikk/quality-gate:latest

      # OPTIONNEL : Kubernetes deployment
      # Uncomment these lines only if you have set KUBECONFIG_CONTENT secret
      # - name: Set up kubeconfig
      #   run: |
      #     mkdir -p $HOME/.kube
      #     echo "${{ secrets.KUBECONFIG_CONTENT }}" > $HOME/.kube/config
      #     kubectl config use-context docker-desktop
      #     kubectl config set-context --current --namespace=ensakh

      # - name: Verify Kubernetes context
      #   run: |
      #     kubectl config current-context
      #     kubectl get namespaces

      # - name: Deploy to kube
      #   run: |
      #     kubectl apply -f k8s/deployment.yaml

      # - name: Verify Kubernetes context and pods
      #   run: |
      #     kubectl config current-context
      #     kubectl get nodes
      #     kubectl get namespaces
      #     kubectl get pods